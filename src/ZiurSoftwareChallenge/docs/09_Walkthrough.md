# 09 - Walkthrough

Quick walkthrough to understand and run the project:

1. Clone repository and open in editor.
2. Set `Api:AuthToken` with `dotnet user-secrets`.
3. `dotnet run --urls http://localhost:5250` and open `/movimientos`.
4. Inspect logs to see `ApiMovimientoService` behavior and the number of items deserialized.
